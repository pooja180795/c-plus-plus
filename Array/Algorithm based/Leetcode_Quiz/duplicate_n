class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        unordered_map<int, int> m;
        for(int val: nums){
            m[val]++;
            if(m[val] == 2){
                return true;
            }
        }
        
        return false;
    }
};
